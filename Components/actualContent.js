import React from "react";
import styled from "styled-components";

const Wrapper = styled.section`
    width: 100%;
    outline: 5px solid green;
    height: calc(100vh - 150px);
`
export default function ActualContent() {
    return (
        <Wrapper>
        </Wrapper>
    );
}